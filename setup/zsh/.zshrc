# Final override escape hatch (~/.local/config/zsh/.zshrc)
# This is the LAST file sourced by .zshrc — everything here takes precedence.
# Use this for one-off overrides that don't fit in env.zsh or aliases.zsh.
